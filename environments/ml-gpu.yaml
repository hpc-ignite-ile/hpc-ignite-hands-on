name: hpc-ignite-ml
# For LANTA live smoke tests, prefer slurm/module-loads/pytorch-shared.sh.
# This file is for custom project-prefix environments prepared before class.
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pytorch>=2.2
  - torchvision
  - torchaudio
  - pytorch-cuda=12.1
  - numpy>=1.24
  - pandas>=2.0
  - matplotlib>=3.7
  - scipy>=1.10
  - scikit-learn>=1.3
  - jupyter>=1.0
  - jupyterlab>=4.0
  - tensorboard
  - tqdm
  - pillow
  - pip
  - pip:
    - transformers>=4.30
    - datasets>=2.14
    - accelerate>=0.21
    - wandb
